# CentralD npm policy. Applies to every project-level npm operation in this
# repository, including the Docker builder images (the repo is copied into the
# build context and this file is picked up by npm ci).
#
# Never install a package version that has been published for less than three
# days. This is a hard supply-chain gate: brand-new releases are not accepted.
min-release-age=3
