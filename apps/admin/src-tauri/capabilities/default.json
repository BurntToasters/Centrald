{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal capability set for the CentralD Admin main window.",
  "windows": ["main"],
  "permissions": ["core:default", "updater:default"]
}
